{
  "name": "incorporation-tool-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "cd backend && ./venv/bin/python -m uvicorn app.main:app --reload",
    "install-all": "npm install --prefix frontend && cd backend && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt"
  }
}
